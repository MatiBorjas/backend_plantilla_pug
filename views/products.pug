html
    head
        title='plantilla pug'
        style
            include styles.css
    body
        h1="listado de productos"
        div
        for item in products
            div
                p=item.id
                p=item.title
                p=item.price
                img(class='product-img', src=item.thumbnail)

    br
    br
    br
    a(href="http://localhost:8080/form" class="btn") Ingresar un nuevo producto
